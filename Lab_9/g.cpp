#include <iostream>

using namespace std;

int main(){
    string s;
    cin >> s;
    for(int i = 0; i < s.size(); i++){
        if(i != s.size() - 1){
            if(s[i] == '1' and s[i+1] == '1')
        }
    }
    return 0;
}